

  const first_ptarget_x = [x, y];
  const first_ptarget_y = a; 
  const first_ppoint_x = a;
  const first_ppoint_y = a; 
  const first_mtarget_x = a; 
  const first_mtarget_y = a; 
  const first_mpoint_x = a; 
  const first_mpoint_y = a; 
  const first_mshoot_x = a; 
  const first_mshoot_y = a; 
  // if()

setPin(first_ptarget_x);
setPin(first_ptarget_x);
setPin(first_ptarget_x);

// let setPin = (function () {

// });
function setPin(x, y) {

  function concat(str) {

  }
}