<div class="modal-dialog" role="document">
  <div class="modal-content">
    <div class="modal-header">
      <h5 class="modal-title" id="new-golfclub-modal-label" style="color: #dc3545;">コース削除</h5>
      <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
    </div>

    <div class="modal-body">
      <p>
        <%= @golfclub.name %>の&nbsp;
        <span style="color: #dc3545;"><%= @course.name %></span>&nbsp;
        コースを<span style="color: #dc3545;">削除</span>しますか？
      </p>
      <%= link_to '削除', golfclub_course_path(@golfclub, @course),
                          remote: true,
                          method: :delete,
                          class: "btn btn-sm btn-danger" %>
    </div>

    <div class="modal-footer">
      <button type="button" class="btn btn-sm btn-secondary" data-dismiss="modal">キャンセル</button>
    </div>
  </div>
</div>
